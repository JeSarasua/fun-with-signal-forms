import { Routes } from '@angular/router';
import { SwFormComponent } from '../components/sw-form/sw-form.component';

export const routes: Routes = [
  {
    title: 'Star Wars Form',
    path: 'form',
    component: SwFormComponent,
  },
];
